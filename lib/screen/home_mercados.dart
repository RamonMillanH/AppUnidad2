import 'package:flutter/material.dart';

class HomeMercados extends StatelessWidget{
  const HomeMercados({super.key});
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mercados'),
      ),
      
    );
  }
}